# Patch 16 fixed

This fixes the failed Patch 16 script and keeps the Review action bar non-sticky.

Run from project root after copying files:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\apply-patch-16-fixed.ps1
```
