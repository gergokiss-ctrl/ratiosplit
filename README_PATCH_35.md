# Patch 35 - Category wording and collapsible panels

Requested changes:

- `Active` wording changed to `Available` / `Available for new expenses`.
- `Delete permanently` moved into a clearer `Danger zone` block.
- `Categories`, `Ordering`, and `Deletion rules` panels are collapsible.
- The collapsed/expanded state of those panels is remembered in localStorage.
- Existing `New category` remembered state is preserved.

No database schema change is required.
