# Patch 11 - PWA polish

This patch improves the mobile/PWA install experience:

- adds proper PWA metadata in `app/layout.tsx`
- updates `public/manifest.json`
- adds 192x192 and 512x512 app icons
- adds Apple touch icon for iPhone home screen usage
- sets theme color and viewport fit for mobile browsers

After deployment, open the app on iPhone Safari and use:

Share -> Add to Home Screen
